# Change Request Tracker

Last updated: 2026-08-01

## Repository Branch Alignment

- Intended invariant: Pasta-Devs `upstream/main` equals Adunato `origin/upstream-main` equals local `upstream-main`.
- Pasta-Devs `upstream/main`, Adunato `origin/upstream-main`, and local `upstream-main` are aligned at `dc0b39282`; local `upstream-main` tracks the Adunato mirror.
- Local `main` is built from current `upstream-main` plus 38 local development commits and is at `7e6743925`, nine commits ahead of Adunato `origin/main` at `a5169b9e0`.
- The replaced Adunato tips are retained locally as annotated tags `archive/2026-07-30/remote/origin-upstream-main-before-alignment` and `archive/2026-07-30/remote/origin-main-before-alignment`.
- Remote alignment completed on 2026-07-30. Archive tags were not pushed and stale remote feature branches were not deleted.

## States

- `standalone`: Work exists on a CR branch but is not merged into `main` and has no open PR into `origin/main`.
- `merged into main`: Work has been merged into local `main`, but not into `origin/main`.
- `PR open into origin main`: A pull request is open against `origin/main`.
- `merged into origin main`: Work has been merged into `origin/main`.
- `archived`: Work is retained for reference only and should not be continued as an active CR.

## CRs

| CR | State | Title | Short Description | Depends On | Notes |
| --- | --- | --- | --- | --- | --- |
| CR001 | archived | Read & Append Chat Summary Built-In Tools | Adds `read_chat_summary` and `append_chat_summary` tools so agents can read and update persisted chat summaries. | None | Superseded by CR004. Archived under `change_requests/archive/CR001_chat_summary_tools/`. |
| CR002 | archived | Universal Agent Tool Support | Enables built-in and custom agents to receive configured tool definitions instead of limiting tool execution to Spotify agents. | None | Merged into `pastadevs/main`. Archived under `change_requests/archive/CR002_universal_agent_tool_support/`. |
| CR003 | archived | Generalized Tool Metadata I/O & Context Repair | Replaces route-specific metadata plumbing with a shared tool context for chat metadata reads, writes, and frontend sync. | CR001, CR002 | Superseded by CR004. Archived under `change_requests/archive/CR003_tool_context_cleanup/`. |
| CR004 | merged into main | Enable Custom Chat Memory Agents | Enables user-built custom agents to maintain chat memory using summary tools, metadata updates, client refresh, and agent cadence controls. | None | Supersedes CR001 and CR003. Rebased onto latest `upstream-main` and included in local `main` for testing. |
| CR005 | merged into origin main | Chat Summary Auto Trim | Adds trusted chat summary snapshot metadata and an optional context mode that excludes already-summarized messages from generation context. | CR004 |  |
| CR006 | archived | E2E Playwright Harness | Adds the Playwright end-to-end smoke harness for application-level validation. | None | Archived on 2026-07-30 after the maintained Playwright harness moved to the parent tools repository. Historical application branch retained as local archive tag `archive/2026-07-30/branch/change/CR006-e2e-playwright-harness`. |
| CR007 | archived | Vector Agent Tools | Assesses a first-class vector store for character-aware and agent-authored memories. | None | Archived on 2026-07-30 as design discovery only. The assessment is retained under `change_requests/archive/CR007_vector_agent_tools/`; the obsolete application branch is retained as a local archive tag. |
| CR008 | standalone | Data Storage Harmonization Discovery | Assesses current file-native storage, semantic search, memory recall, trackers, memory commands, and adjacent persisted narrative data to design a cohesive library storage model. | None | Design/discovery only; no application branch opened yet. |
| CR009 | archived | Agent Memory Enhancement | Enhances existing agent memory beyond narrow per-chat key/value storage with save, search, list, and delete tools, while evaluating whether to supersede or extend `agent_memory`. | CR008 | Archived on 2026-07-30 after the fork PRs merged but the upstream contribution closed without merge and the implementation fell behind current upstream. Documentation moved under `change_requests/archive/CR009_agent_memory_enhancement/`; implementation retained as local archive tags for future redesign from current `main`. |
| CR010 | merged into main | Roleplay Context Sources | Lets a Roleplay use multiple Conversation, Roleplay, or Game chats as read-only summary and recent-message context. | None | Implemented in `681e8ce6`; merged into application `main` by `cd14732a`. |
| CR011 | merged into main | Conversation Custom Tracker | Extends the existing Roleplay Custom Tracker to Conversation mode with multiple user-defined fields, existing snapshot persistence, committed prompt context, and a Conversation-appropriate editor. | None | Implemented in `21351e48` and merged by `fe50fb5a`; picker fix `cf05f215` passed focused Playwright E2E and was merged by `bc56b622`. Official package description metadata remains a companion `Pasta-Devs/Marinara-Agents` follow-up. |
| CR012 | merged into main | Expanded Tracker Panel | Makes the desktop Tracker panel's expanded profile materially larger while keeping compact, standard, tracker behavior, and data unchanged. | CR011 | Expanded target changed from 420px to 840px in `6a4d94cf`; merged into custom application `main` by `df1d4a11`. |
| CR013 | archived | Scene Conversation Context Continuity | Gives `/scene` planning and the created roleplay the same summary-rich history and full current-day context available to normal Conversation generation. | None | Closed on 2026-07-26 after implementation in `d62923e8`, fast-forward merge into local application `main`, focused validation, and primary-checkout build. Archived under `change_requests/archive/CR013_scene_conversation_context/`. |
| CR014 | merged into main | Full Cross-Chat Conversation Context | Gives a Conversation summaries and current logical-day messages from enabled Conversations sharing any current character, with explicit source and speaker attribution. | None | Implemented in `8588a307`; shared-character boundary restored in `d1c7cb1a`; source logical-day window corrected in `380d93fd`, validated, built in the primary checkout, and merged into local application `main`. |
| CR015 | merged into main | Daily Conversation Memories | Adds a built-in agent and Conversation UI for day-scoped memories that complement summaries and can be used with or instead of current memory recall. | None | Implemented in `5ff1fbcb`, participant-ID validation in `8da9b278`, Conversation picker exposure in `3dd20d49`, UI/configuration refinement in `15f12f2b`, read-only retrieval preview in `33519ceb`, daily-memory-only preview correction in `59f10e4c`, chronological/uncapped preview refinement in `355a522c`, editable-card scroll correction in `5aea5ea2`, and configurable minimum-rank filtering in `941378ab`; focused API/UI Playwright validation passed, including uncapped retrieval, minimum-rank filtering, ordered consecutive settings saves, chronological grouping, card/textarea-surface scrolling at desktop and mobile widths, Conversation configuration, compact/themed editor presentation, save persistence, and animated generation progress. The full app check and primary checkout production build passed. |
| CR016 | merged into main | Daily Intentions | Adds manually generated, editable first-person daily intentions across four configurable life areas for single-character Conversations, with current-only context injection. | CR015 | Implemented in `24de9d16`; context corrected in `ece0465c` to use a rolling 24-hour message window plus summaries and persisted Daily Memories, never last-N prompt context; managed add/editor controls corrected in `3bee031f` to describe that fixed contract and omit generic agent budgets/shared prompts. Focused API/UI Playwright validation passed 4/4 for the 23-hour/25-hour boundary, fixed configuration, sequential partial success, failed-value preservation, clean immutable context, injection, multi-character blocking, mobile add-dialog controls, and manual configuration/run/edit flows. Client lint and the primary-checkout production build passed. Initial scope remains manual-only; the visible cutoff is reserved for future scheduling. |
| CR017 | merged into main | Conversation Auto-Summary Controls | Adds a per-Conversation summary connection and a prompt-only control for including or excluding auto-summary memories while always retaining summary injection. | None | Implemented in `076b1acda`, validated with focused regressions, type checks, changed-file ESLint, production build, and 2/2 focused Playwright tests, then fast-forwarded into local application `main`. The primary checkout production build passed after integration. |
| CR018 | merged into main | Scene Daily Memory Snapshot Continuity | Extends `/scene` planning and the created Roleplay with the origin Conversation's current ranked Daily Memories retrieval snapshot, frozen into CR013's existing context capture. | CR013, CR015 | Implemented in `afbb2398f`; focused scene-context regression and server TypeScript validation passed. The user chose integration without added Playwright E2E, local application `main` was fast-forwarded to the implementation commit, and primary-checkout build artifacts were generated for that commit before the top-level build wrapper timed out. |
| CR019 | merged into main | Compiled Character Mind | Adds an opt-in, per-character Conversation agent implementing Karpathy's raw sources, Markdown wiki, schema, and ingest/query/lint operations in isolation. | CR015 | Implemented in `4dd9b0cdf` and fast-forwarded into local application `main`; focused Character Mind regression, shared/server TypeScript validation, and the primary-checkout production build passed. Query returns a standalone detailed cited briefing; response-generation integration and all custom mind UI remain deferred. |
| CR020 | merged into main | Character Mind Controls | Adds the minimal Conversation UI needed to configure and operate CR019 without adding a Markdown browser or editor. | CR019 | Implemented in `8e4709d24`; visibility fix `eb05263b4` added Character Mind to the Conversation agent allowlist. Focused Character Mind and Conversation policy regressions, changed-file client lint, client/server/shared TypeScript checks, and integrated primary-checkout production builds passed. The broad `pnpm check` exceeded its command window during full client lint and was not repeated. |
| CR021 | merged into main | Character Mind Runtime Hardening | Makes Character Mind tool and result contracts explicit and surfaces unresolved mutation failures instead of secondary terminal-format errors. | CR019, CR020 | Diagnosed from Phoenix trace `d4850e5a1e0721befc4df86da8540894`; implemented in `447c0cb8a`. Focused Character Mind regression, server TypeScript validation, and the primary-checkout production build passed. |
| CR022 | merged into main | Corpus-Aware Character Mind Build | Replaces source-ordered initial ingest with a corpus-wide page-map pass followed by mapped wiki materialization, and adds existing Conversation auto-summaries as raw sources. | CR019, CR020, CR021 | Implemented in `5b7fd02b7` and fast-forwarded into local application `main`. Build now maps current Character Card, day/week auto-summaries, and formed Daily Memories before writing pages; Sync remains incremental only after a successful Build. Focused Character Mind regression and the full `pnpm check` passed; the only lint output was one pre-existing React Hooks warning in `HomeProfessorMariChat.tsx`. |
| CR023 | merged into main | Character Mind Plan Recovery | Lets the corpus planner correct incomplete reads or an invalid proposed map within the same agent run instead of failing only after finalization. | CR022 | Diagnosed from Phoenix trace `4ca29a157a16956a914d874fa0c56c55`; implemented in `1188cbdd4`. Focused Character Mind regression, server TypeScript validation, and the integrated primary-checkout production build passed. |
| CR024 | merged into main | Shared LLM Transport Retries | Adds bounded transparent retries for transient provider-request failures while preserving the calling agent or workflow state. | None | Implemented in `7e6743925` and fast-forwarded into local application `main`. The provider compatibility and deterministic retry regressions passed, as did shared/server TypeScript validation; the single full `pnpm check` attempt reached its four-minute limit during the client phase and was not repeated. |
| CR025 | merged into main | Character Mind Page Sessions | Materializes each frozen Character Mind page in its own sequential agent session while retaining the corpus-wide map. | CR022, CR023, CR024 | Implemented in `20fbdd4f6` and fast-forwarded into local application `main`. Focused Character Mind regression, server TypeScript validation, and the integrated primary-checkout production build passed. |
| CR026 | merged into main | Character Mind Page Streaming | Streams Character Mind page materialization while preserving its existing validated tool-call workflow. | CR024, CR025 | Implemented in `0085c2074` and fast-forwarded into local application `main`. Focused Character Mind regression, server TypeScript validation, and the integrated primary-checkout production build passed. |
| CR027 | merged into main | Phoenix Raw Stream Tracing | Captures exact OpenAI-compatible streamed response bodies and parsing diagnostics in opt-in Phoenix LLM spans. | CR026 | Implemented in `a9432755a`, validated with provider regressions and server TypeScript checks, built in the primary checkout, and fast-forwarded into local application `main`. Raw streams may contain complete model output. |
| CR028 | merged into main | Cross-Chat Daily Memory Awareness | Adds each qualifying source Conversation's complete last Daily Memories retrieval result to Cross-Chat Awareness. | CR014, CR015 | Implemented in `1e5be8dfd` and fast-forwarded into local application `main`. Focused Cross-Chat Awareness regression, server TypeScript validation, and the integrated primary-checkout production build passed. |
| CR029 | merged into main | Resumable Character Mind Builds | Persists the frozen corpus map in Markdown, resumes incomplete page materialization, preloads mandatory files, and applies timeouts per provider request. | CR022, CR025, CR026 | Implemented in `d04fd930f` and fast-forwarded into local application `main`. Focused Character Mind regression, server TypeScript validation, changed-file client ESLint, client production build, and the integrated primary-checkout production build passed. The single broad `pnpm check` attempt reached its command window during the client phase and was not repeated. |
| CR030 | standalone | Staged Character Mind Editing | Replaces complete-content write-tool arguments with streamed page creation/replacement, bounded candidate edits, validation, and manual-edit conflict checks while preserving existing retries and Build resume. | CR019, CR021, CR022, CR024, CR025, CR026, CR029 | Implemented in application commit `a4e67dd88` on `change/CR030-staged-character-mind-editing`. Focused Character Mind regression and server TypeScript validation passed; the single broad `pnpm check` attempt reached its command limit during the client build and was not repeated. Awaiting merge and manual validation. |
